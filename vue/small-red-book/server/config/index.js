// 声明好mysql
const config = {
    database: {
        DATABASE: 'smallredbook',
        USERNAME: 'root',
        PASSWORD: '123456',
        PORT: '3306',
        HOST: 'localhost' 
    }
}

module.exports = config